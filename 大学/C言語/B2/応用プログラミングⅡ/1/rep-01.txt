/* 学生番号 T22J209*/
/* 氏名 伊藤駿介*/
/* 処理は１行で記述する必要はない */

// レポート課題 rep1-1.c

#include <stdio.h>
int main(void)
{
   int a=0, b=100, tmp; /* 最初の指定 */
   /* (1) */

   printf("befor:a=%d, b=%d\n", a, b);

   /* (2) */

   /* (3) */

   printf("after:a=%d, b=%d\n", a, b);
   return 0;
}

// rep1-1の実行結果


// レポート課題 rep1-2.c
#include <stdio.h>
int main(void)
{
   double a=-1.1, b=4.2, c=-0.1; /* 最初の指定 */
   /* (1) */

   /* (2) */

   /* (3) */
  
   printf("最大値は%f\n", *pMax);

   return 0;
}

// rep1-2の実行結果