#include <bits/stdc++.h>
using namespace std;

int main(){
    int Sx, Sy, Tx, Ty, x, y, count = 0;
    bool right = false;
    cin >> Sx >> Sy >> Tx >> Ty;

    //初期位置
    Sx =+ Sx + 0.5;
    Sy =+ Sy + 0.5;
    //現在位置の初期化
    x = Sx;
    y = Sy;

    if(Sx % 2 == 0){
        right = false;
    }

    //ゴール
    Tx =+ Tx + 0.5;
    Ty =+ Ty + 0.5;

    while(x == Tx){
        while(y == Ty){
            y++;
        }
        if(right = )
    }

    for(int i=0; i < Tx + Ty; i++){
        
    }
}